# Сутності Домену

## 1. Task (Завдання)
* **Опис**: Ключова сутність, що представляє одиницю роботи.
* **Атрибути**:
    * `id` — унікальний ідентифікатор
    * `name` — текст завдання
    * `status` — статус (`pending` або `completed`)
